;
; BIND reverse data file for local loopback interface
;
$TTL   604800
@      IN       SOA    {{ sub_domain }}. root.{{ domain }}. (
                               2          ; Serial
                          604800          ; Refresh
                           86400          ; Retry
                         2419200          ; Expire
                          604800 )        ; Negative Cache TTL

;Name Server Information
@      IN       NS     {{ sub_domain }}.
;Reverse lookup for Name Server
{{ last_ip }}      IN       PTR    {{ domain }}.
